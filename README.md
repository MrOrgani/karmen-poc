# Karmen POC — Cockpit Analyste

## Démarrage

```bash
npm install
npm run dev
```

Lance simultanément :
- **Backend** (NestJS) sur http://localhost:3000
- **Frontend** (Vite + React) sur http://localhost:5173

Le frontend proxy `/api/*` vers le backend.
